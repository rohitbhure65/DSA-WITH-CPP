#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_set<int> st;
    // lower_bound and upper_bound function
    // does not works, rest all functions are same
    // it store unique but does't stores any
    // particular order it has a better complexity
    // then set in most cases, expect some when collision happens
    return 0;
}